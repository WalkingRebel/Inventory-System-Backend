# Inventory Management System (FastAPI)

## Tech Stack
-FastAPI
-PostgreSQL
-SQLAlchemy
-OAuth + JWT
-uv(Package Manager)

## Setup
 ```bash
 uv venv
 .venv\Scripts\activate
 uv sync
 uv run uvicorn app.main:app --reload
 